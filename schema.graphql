type Maturity @entity {
  id: ID!
  symbol: String!
  name: String!
  maturity: BigInt!
  totalSupply: BigDecimal!
  pool: Pool @derivedFrom(field: "maturity")
}

type Pool @entity {
  id: ID!
  maturity: Maturity!
  totalVolumeDai: BigDecimal!
}
